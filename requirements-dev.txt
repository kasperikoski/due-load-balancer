# Development-only dependencies for Due Load Balancer.
# These are for tests, linting and local project maintenance.
# The Anki add-on itself does not need these packages when installed in Anki.

pytest>=8.0
pytest-cov>=5.0
ruff>=0.8.0
mypy>=1.10
typing-extensions>=4.12