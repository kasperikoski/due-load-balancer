"""Central constants for the add-on."""

from __future__ import annotations

ADDON_VERSION = "0.0.1"
DEFAULT_PROJECT_NAME = "Due Load Balancer"
DEFAULT_LANGUAGE = "en"

# In Anki's card queue values, review cards are queue 2. New and learning cards
# use different queues and are intentionally ignored by this add-on.
REVIEW_QUEUE = 2
